# Premium Game Options Upgrade

Applied upgrades:
- Replaced basic radio option feel with premium animated choice cards
- Added hover lift, neon border glow, selected-card pulse, and smooth transitions
- Added animated submit feedback cards
- Added success/failure scoring chips
- Kept root launcher: `python -m streamlit run app.py`

Run:
```bash
pip install -r requirements.txt
python -m streamlit run app.py
```
